{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "test -d packages/database && npm install -w apps/web -w apps/api -w packages/database -w packages/validation --include-workspace-root || (cd ../.. && npm install -w apps/web -w apps/api -w packages/database -w packages/validation --include-workspace-root)",
  "buildCommand": "next build",
  "outputDirectory": ".next"
}
